import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Cloudflare Pages + @cloudflare/next-on-pages 배포용
  // 참고: https://github.com/cloudflare/next-on-pages
};

export default nextConfig;
